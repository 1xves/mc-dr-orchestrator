# Do not commit with real values — use kubectl create secret or external-secrets
# Apply with: kubectl create secret generic postgres-credentials \
#   --namespace=database \
#   --from-literal=POSTGRES_PASSWORD=<your-password> \
#   --from-literal=POSTGRES_USER=drapp \
#   --from-literal=POSTGRES_DB=drapp
#
# This file is a reference template only.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: database
type: Opaque
stringData:
  POSTGRES_USER: drapp
  POSTGRES_DB: drapp
  POSTGRES_PASSWORD: REPLACE_ME   # override at apply time, never commit real value
