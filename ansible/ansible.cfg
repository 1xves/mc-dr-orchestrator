[defaults]
inventory = inventory/hosts.yml
gathering = explicit
callback_result_format = yaml
interpreter_python = auto_silent
# Both clusters are driven from the controller over the Kubernetes API;
# there is no SSH inventory in this project.
